fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
python-multipart==0.0.17
pillow==11.0.0
aiofiles==24.1.0

# face_recognition_models imports pkg_resources at module load. Python 3.12
# venvs no longer ship setuptools, and setuptools>=81 removes pkg_resources,
# so without this pin `import face_recognition` fails outright.
setuptools<81

face_recognition==1.3.0
dlib==20.0.1
numpy==1.26.4

# Headless build: the only cv2 calls are imwrite/cvtColor, so the GUI libs are
# dead weight. Saves ~25 MB resident and a large chunk of the container image.
opencv-python-headless==4.10.0.84
